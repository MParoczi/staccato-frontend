# Milestone v0.1: Foundation

**Status:** ✅ SHIPPED 2026-05-16
**Phases:** 1
**Total Plans:** 5
**Commits:** 93d8b49 → d631ee9 (14 commits)

## Overview

Foundation sprint delivering the bootable, routed, observable app skeleton for Staccato. No user-facing features — this milestone establishes the technical platform all subsequent phases depend on: Vite 8 project scaffold, TypeScript strict config, Tailwind v4 CSS-first setup, shadcn primitives, routing structure, i18n bootstrap, Axios instance with interceptors, Zustand authStore, QueryClient, ProtectedRoute, and environment variable validation.

## Phases

### Phase 1: Foundation

**Goal:** Bootable app skeleton — Vite project, TypeScript config, Tailwind v4 CSS-first setup, shadcn primitives, routing structure, i18n bootstrap, Axios instance with interceptors, Zustand stores, QueryClient, ProtectedRoute, environment variable validation
**Mode:** mvp
**Requirements:** Infrastructure (enables all subsequent phases; no explicit REQ-IDs)
**Plans:** 5 plans

Plans:

- [x] 01-PLAN-01.md — Project scaffold: Vite 8 + TypeScript 5.9.3 + Tailwind v4 + shadcn radix-nova (17 components) + Zod env validation
- [x] 01-PLAN-02.md — State and HTTP: Zustand authStore (no persist) + Axios client.ts (interceptors) + rawClient.ts
- [x] 01-PLAN-03.md — i18n: i18next v26 init module + 16 translation JSON files (8 namespaces × en + hu)
- [x] 01-PLAN-04.md — Walking skeleton: createBrowserRouter + ProtectedRoute (spinner) + main.tsx boot sequence
- [x] 01-PLAN-05.md — Test suite: Vitest setup + unit tests (authStore, env, ProtectedRoute, Axios, i18n) + smoke test

**Cross-cutting constraints enforced:**
- `pnpm tsc --noEmit` exits 0 after every plan wave
- Access token never appears in localStorage, sessionStorage, or persist middleware
- All page navigation via React Router — never `window.location.href`
- pnpm only

**Success Criteria — all PASS:**
1. `pnpm dev` starts; `pnpm build` completes with zero TS + zero lint errors ✅
2. `/` redirects unauthenticated → `/login`; authenticated `/login`+`/register` → `/app/notebooks` ✅
3. All route paths resolve to correct page shells ✅
4. `useAuthStore` has no `persist` middleware; store resets to null on reload ✅
5. i18n initialises English default; `t('appName')` resolves; `Accept-Language: en` on every request ✅

**UAT:** Complete (uat_status: complete, 2026-05-16)

---

## Milestone Summary

**Timeline:** 2026-05-15 → 2026-05-16 (2 days)
**Git range:** 93d8b49–d631ee9
**Files:** 450 files, 102,399 insertions

**Key Decisions:**

- shadcn@4.6.0 used for init (latest had workspace config bug); latest shadcn works for add commands
- radix-nova style uses unified radix-ui package (not @radix-ui/* individual packages)
- form.tsx created manually (not in radix-nova shadcn registry)
- TypeScript pinned to 5.9.3 (Vite scaffold would install 6.0.3 by default)
- tsconfig.json root given compilerOptions.paths to satisfy shadcn CLI alias validation
- vitest.config.ts separate from vite.config.ts to avoid @tailwindcss/vite in jsdom environment
- tsconfig.app.json excludes test files so pnpm build does not type-check test-only patterns
- i18n test uses i18next.createInstance() with inline resources (no http-backend side effects)
- Boot refresh via rawClient (not client) before ReactDOM.render — prevents login-page flash
- authStore starts with status: 'loading' (not 'unauthenticated') — prevents flash before boot refresh
- module-level refreshPromise for single-flight 401 token refresh (not inside error handler)

**Issues Resolved:**

- pnpm create vite --overwrite deleted untracked files (.planning/, .claude/); restored from git
- shadcn form component not in radix-nova registry — created manually
- shadcn@latest init had workspace config error — used shadcn@4.6.0
- tsconfig.json needed paths for shadcn CLI alias validation
- ESLint errors in shadcn-generated files (badge, button, form) — eslint-disable comments
- .claude tooling files picked up by ESLint — added globalIgnores
- Missing .env.local caused blank page on boot — created with VITE_API_BASE_URL placeholder

**Issues Deferred:**

- Hungarian locale strings — all set to __HU_TODO__ placeholders, resolved in Phase 12
- VALIDATION.md nyquist_compliant: false — Nyquist audit not formally completed (UAT served as verification)

**Technical Debt Incurred:**

- Page stubs (LoginPage, RegisterPage, NotebooksPage, RootPage) are empty shells; filled in phases 2+
- No barrel files — imports are direct; consistent with no-barrel convention
- .env.local not committed (in .gitignore) — must be created manually on new machine

---

*For current project status, see .planning/ROADMAP.md*
