# Requirements Archive: v0.3 User Profile & Account

**Milestone:** v0.3 User Profile & Account
**Phase:** 3
**Archived:** 2026-05-16
**Status:** ✅ All requirements shipped

---

## Traceability

| REQ-ID | Description | Status | Outcome |
|--------|-------------|--------|---------|
| NAV-01 | AppLayout with persistent navbar wrapping all `/app/*` routes | ✅ Shipped | `AppLayout.tsx` + `Navbar.tsx` created; nested in router as pathless layout route |
| USER-01 | Profile view & edit (firstName, lastName, language, defaultPageSize, defaultInstrumentId) | ✅ Shipped | `ProfilePage.tsx` + `PATCH /users/me`; form pre-populated from `GET /users/me` |
| USER-02 | Avatar upload (JPG/PNG/WebP ≤ 2 MB) with immediate navbar reflection | ✅ Shipped | `POST /users/me/avatar`; client-side 2 MB guard; `authStore.updateUser` syncs navbar |
| USER-03 | Request account deletion with 30-day grace period dialog + banner | ✅ Shipped | `POST /users/me/deletion`; confirmation Dialog; `scheduledDeletionAt` banner |
| USER-04 | Cancel scheduled account deletion from banner | ✅ Shipped | `DELETE /users/me/deletion`; banner disappears on success; Sonner toast confirms |

All 5 requirements: **validated in production UAT (13/13 tests passed)**.

---

## Original Requirements

### NAV-01 — AppLayout with Navbar

A persistent `AppLayout` component wraps all authenticated routes. The top navbar displays the Staccato logo and a user avatar button that opens a dropdown menu.

**Behavior:**
- Avatar button renders `avatarUrl` image when available; falls back to shadcn `Avatar` initials (first char of `firstName` + first char of `lastName`, or first char of `displayName` when names are not set)
- Dropdown options: "My Profile" → `navigate('/app/profile')`, "Sign out" → existing logout flow (`clearAuth` + navigate to `/login`)
- `AppLayout` replaces the current bare `ProtectedRoute` child structure — all `/app/*` routes render inside it
- `avatar.tsx` shadcn component is already installed; `dropdown-menu.tsx` is already installed

**API:** No new endpoints — avatar URL sourced from `authStore.user.avatarUrl`

---

### USER-01 — Profile View & Edit

User can view and edit their profile fields: `firstName`, `lastName`, `language`, `defaultPageSize`, `defaultInstrumentId`.

**Route:** `GET /app/profile`

**Outcome:** Fully implemented. Language preference calls `i18next.changeLanguage()` on save. Instrument list loaded from `GET /instruments`. All fields validated with Zod (firstName/lastName ≤ 100 chars).

---

### USER-02 — Avatar Upload

User can upload a custom avatar (JPG/PNG/WebP, ≤ 2 MB).

**Outcome:** Fully implemented. Client-side 2 MB guard rejects before upload. Loading spinner shown during upload. `authStore.updateUser` updates navbar avatar on success.

---

### USER-03 — Request Account Deletion

User can initiate a 30-day account deletion grace period from the profile page.

**Outcome:** Fully implemented. Confirmation Dialog required before action. Warning banner with scheduled date appears after confirmation.

---

### USER-04 — Cancel Account Deletion

User can cancel a pending account deletion from the warning banner on the profile page.

**Outcome:** Fully implemented. Cancel button in banner calls `DELETE /users/me/deletion`. Banner disappears on success. No secondary confirmation required (recovery action).

---

## Open Questions Resolution

| # | Question | Resolution |
|---|----------|------------|
| Q1 | Avatar upload endpoint: `POST` or `PATCH /users/me/avatar`? | `POST /users/me/avatar` (confirmed from API spec) |
| Q2 | `GET /instruments` — separate endpoint or static? | Separate endpoint exists; `InstrumentOption[]` returned |
| Q3 | `UserProfile.defaultInstrument: string` — reconcile to `defaultInstrumentId: Guid?` | Reconciled in PLAN-01 (`string | null`) |

---

_Archived from .planning/REQUIREMENTS.md at v0.3 milestone close — 2026-05-16_
