# Requirements Archive: v0.5 Lessons & Pages

**Milestone:** v0.5 Lessons & Pages
**Phase:** 5
**Archived:** 2026-05-17
**Status:** ✅ All requirements shipped

---

## Traceability

| REQ-ID | Description | Status | Outcome |
|--------|-------------|--------|---------|
| LES-01 | Create lesson within a notebook; auto-navigates; backend auto-creates first page | ✅ Shipped | `CreateLessonDialog.tsx` + `LessonsPage.tsx` — `POST /notebooks/{id}/lessons`, cache invalidated, auto-navigate to lesson |
| LES-02 | Rename lesson; updated title reflected immediately in lesson list | ✅ Shipped | `RenameLessonDialog.tsx` — `PUT /lessons/{id}`, invalidates `['lessons', notebookId]` and `['lesson', lessonId]` |
| LES-03 | Delete lesson with irreversible confirmation; cascade removes pages | ✅ Shipped | `DeleteLessonDialog.tsx` — confirmation copy includes lesson title + "This can't be undone"; `DELETE /lessons/{id}` |
| LES-04 | Lesson list at `/lessons` tab; LessonPage shell with page counter, global number, prev/next; breadcrumb | ✅ Shipped | `LessonsPage.tsx` + `LessonPage.tsx` — URL-based navigation (?page=N), 3-level Navbar breadcrumb |
| PAGE-01 | Add page; soft 10-page warning toast; page counter increments | ✅ Shipped | Add page button in `LessonPage.tsx` — `POST /lessons/{id}/pages`, Sonner warning at pageCount >= 10 |
| PAGE-02 | Delete page disabled on last page; correct global page numbers after deletion | ✅ Shipped | `DeletePageDialog.tsx` — button disabled when `pageCount === 1`; global number recalculated from ordered lesson list |

All 6 requirements: **validated in production UAT (10/10 tests passed)**.

---

## Original Requirements

### LES-01 — Create Lesson

Users can create a lesson within a notebook. The lesson appears in the lesson list ordered by creation date. The first page is auto-created by the backend when the lesson is created.

**Acceptance:**
- `POST /notebooks/{id}/lessons` is called on form submit with `{ title }` payload
- Title is required; form validates before submit; submit button disabled while in flight
- Newly created lesson appears in the lesson list immediately (TanStack Query cache invalidated)
- Lessons are displayed in `createdAt` ascending order (oldest first)
- The new lesson navigates the user to the lesson view (or the list scrolls to reveal it)
- First page exists on the lesson without a separate page-creation step (backend guarantees this)

**Outcome:** Fully implemented. `CreateLessonDialog.tsx` validates title, calls `createLesson`, invalidates lessons cache, and auto-navigates to `/app/notebooks/:id/lessons/:lessonId`. Backend confirmed to auto-create first page.

---

### LES-02 — Rename Lesson

Users can rename a lesson. The updated title is reflected immediately in the lesson list.

**Acceptance:**
- `PUT /lessons/{id}` is called with `{ title }` payload
- Title is required; empty title is not submitted
- Lesson list reflects the new title without a full page reload (TanStack Query cache updated)
- Rename can be triggered from the lesson list row (inline edit or dialog)
- Mutation errors show exactly one Sonner toast (consistent with ERR-01 pattern from v0.4)

**Outcome:** Fully implemented. `RenameLessonDialog.tsx` pre-fills current title, calls `updateLesson`, and invalidates both `['lessons', notebookId]` and `['lesson', lessonId]` caches. Bug found and fixed during UAT: initial implementation only invalidated the list cache, leaving stale title in LessonPage header.

---

### LES-03 — Delete Lesson

Users can delete a lesson. Deletion is permanent and requires confirmation. All pages and modules are removed by the backend (cascade).

**Acceptance:**
- A confirmation dialog is shown before `DELETE /lessons/{id}` is called
- Confirmation copy includes the lesson title and "This can't be undone" (consistent with v0.4 notebook delete)
- Lesson disappears from the list immediately after deletion (cache invalidated)
- If the user is currently viewing the deleted lesson, they are navigated back to the lesson list
- Mutation errors show exactly one Sonner toast

**Outcome:** Fully implemented. `DeleteLessonDialog.tsx` matches `DeleteNotebookDialog` pattern from v0.4. Confirmation copy includes lesson title and "This can't be undone" from locale. On success, navigates to lesson list. Backend cascade confirmed.

---

### LES-04 — Lesson Navigation

Users can open a lesson and see a page navigation shell. Each lesson page is accessible via previous/next navigation. The lesson URL follows the pattern `/app/notebooks/:id/lessons/:lessonId`.

**Acceptance:**
- Clicking a lesson in the lesson list navigates to `/app/notebooks/:id/lessons/:lessonId`
- The lesson view shows: lesson title, current page number (e.g. "Page 1 of 3"), previous/next page buttons
- The current page's global page number is displayed (e.g. "Notebook p. 5")
- An empty canvas placeholder is shown for the page content area (canvas implemented in Phase 6)
- Back navigation returns to the lesson list
- The "Lessons" tab in `NotebookPage.tsx` is enabled (remove `disabled: true`)

**Outcome:** Fully implemented. `LessonPage.tsx` uses `useSearchParams` (?page=N, 1-based) for URL-driven navigation. Global page number bug fixed during UAT (backend `globalPageStart` is 0-indexed — added +1). Navbar updated with 3-level breadcrumb (Notebooks › [Notebook] › [Lesson]). Lessons tab enabled.

---

### PAGE-01 — Add Page

Users can add a new page to a lesson. A soft warning toast appears when a lesson reaches 10 pages. The page count increments in the lesson list.

**Acceptance:**
- `POST /lessons/{id}/pages` is called when the user clicks "Add page"
- The new page appears immediately; the user is navigated to the new page
- The page counter in the lesson list row increments (TanStack Query invalidates `['lessons', notebookId]`)
- When a lesson reaches exactly 10 pages, a Sonner warning toast is shown
- The "Add page" button remains enabled above 10 pages (warning is soft, not blocking)

**Outcome:** Fully implemented. Bug found and fixed during UAT: backend `POST /lessons/{id}/pages` response does not include `pageNumber` field; navigation now computes `totalPages + 1` client-side. Warning toast fires at `pageCount >= 10` (soft limit, page still added).

---

### PAGE-02 — Delete Page

Users cannot delete the last remaining page of a lesson. Global page numbers display correctly across multiple lessons.

**Acceptance:**
- `DELETE /lessons/{lessonId}/pages/{pageId}` is called when the user confirms page deletion
- The "Delete page" button is disabled (or hidden) when `pageCount === 1`
- If the backend returns a 4xx on last-page delete, a Sonner error toast is shown as fallback
- After deletion, the user is navigated to the previous page (or page 1 if on page 1)
- Global page numbers are recalculated and displayed correctly after page deletion
- The page counter in the lesson list decrements immediately (cache invalidated)

**Outcome:** Fully implemented. Delete page button disabled (not hidden) when `pageCount === 1` — maintains discoverable UI. `DeletePageDialog.tsx` invalidates lesson, lessonPages, and lessons list caches. Navigation lands on adjacent page after deletion.

---

## Open Questions — Resolved

| # | Question | Resolution |
|---|----------|------------|
| Q1 | Does `POST /notebooks/{id}/lessons` auto-create the first page? | ✅ Yes — backend auto-creates first page; no separate `POST /lessons/{id}/pages` call needed |
| Q2 | Does `Lesson` response include `globalPageStart`? | ✅ Yes, but it is 0-indexed — frontend adds +1 for display |
| Q3 | Does `LessonPage` response include `globalPageNumber`? | ✅ Backend provides `globalPageStart` on Lesson; `pageNumber` field on LessonPage response is absent — computed client-side |
| Q4 | What HTTP status does backend return on last-page delete? | Resolved by frontend gate: delete button disabled at `pageCount === 1`; backend 4xx serves as fallback only |
