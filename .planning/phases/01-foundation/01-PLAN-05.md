---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - vitest.config.ts
  - src/stores/__tests__/authStore.test.ts
  - src/__tests__/env.test.ts
  - src/__tests__/i18n.test.ts
  - src/components/ui/__tests__/ProtectedRoute.test.tsx
  - src/api/__tests__/client.test.ts
  - src/pages/__tests__/loginRegisterRedirect.test.tsx
  - tests/smoke/app-boots.test.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "pnpm test passes with all Phase 1 unit and smoke tests green"
    - "authStore starts with status 'loading' and has no persist middleware (verified by test)"
    - "Env validation throws when VITE_API_BASE_URL is missing (verified by test)"
    - "ProtectedRoute renders Loader2 when status='loading' (verified by test)"
    - "ProtectedRoute renders Navigate to /login when status='unauthenticated' (verified by test)"
    - "Axios client attaches Authorization header when token is present (verified by test)"
    - "App smoke test: renders without crashing (verified by test)"
    - "LoginPage and RegisterPage redirect authenticated users to /app/notebooks (verified by test, ROADMAP SC2)"
    - "i18n initializes and t('common.appName') resolves to 'Staccato' (verified by test)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config for jsdom environment"
      contains: "jsdom"
    - path: "src/stores/__tests__/authStore.test.ts"
      provides: "authStore unit tests"
      contains: "status.*loading"
    - path: "src/components/ui/__tests__/ProtectedRoute.test.tsx"
      provides: "ProtectedRoute unit tests"
      contains: "Loader2"
    - path: "tests/smoke/app-boots.test.tsx"
      provides: "App smoke test"
      contains: "renders"
    - path: "src/__tests__/i18n.test.ts"
      provides: "i18n initialization test"
      contains: "common.appName"
  key_links:
    - from: "vitest.config.ts"
      to: "src/stores/__tests__/*.test.ts"
      via: "vitest test discovery"
      pattern: "include.*test"
---

<objective>
Install Vitest and write the Phase 1 test suite: unit tests for authStore, env validation, ProtectedRoute, and Axios client interceptors, plus a smoke test confirming the app renders without crashing.

Purpose: Phase 1 success criteria require automated verification. These tests become the regression baseline for all subsequent phases. The RESEARCH.md Validation Architecture section defined exactly which test files are needed — this plan creates them all.

Output: vitest.config.ts, Vitest + testing-library installed, 8 test files covering Phase 1 behaviors. All tests pass.
</objective>

<execution_context>
@C:/Users/shift/Desktop/Frontend/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shift/Desktop/Frontend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/shift/Desktop/Frontend/.planning/PROJECT.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-RESEARCH.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-01-SUMMARY.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-02-SUMMARY.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<interfaces>
<!-- From src/stores/authStore.ts (Plan 02) -->
From src/stores/authStore.ts:
```typescript
type AuthStatus = 'loading' | 'authenticated' | 'unauthenticated'
export const useAuthStore: // Zustand store
// Initial state: { status: 'loading', user: null, accessToken: null }
// Actions: setAuth(user, accessToken), clearAuth(), setLoading()
```

From src/api/client.ts (Plan 02):
```typescript
export const client: AxiosInstance
// request interceptor: reads useAuthStore.getState().accessToken → sets Authorization header
// response interceptor: 401 → single-flight refresh
```

From src/components/ui/ProtectedRoute.tsx (Plan 04):
```typescript
export function ProtectedRoute(): JSX.Element
// Renders Loader2 when status === 'loading'
// Returns <Navigate to="/login" replace /> when status === 'unauthenticated'
// Returns <Outlet /> when status === 'authenticated'
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Vitest and create vitest.config.ts</name>

  <read_first>
    C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-RESEARCH.md — Validation Architecture section (test framework: Vitest; Wave 0 gaps list; quick run command: pnpm vitest run --reporter=verbose)
    C:/Users/shift/Desktop/Frontend/vite.config.ts — current Vite config (vitest.config.ts can reference its plugins)
    C:/Users/shift/Desktop/Frontend/package.json — current scripts section
  </read_first>

  <action>
    Step 1 — Install Vitest and testing dependencies:
    pnpm add -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @vitest/coverage-v8

    Step 2 — Create vitest.config.ts at project root:
    Import defineConfig from 'vitest/config'. Export defineConfig with:
    - test.environment: 'jsdom'
    - test.globals: true
    - test.setupFiles: ['./src/test-setup.ts']
    - test.include: ['src/**/*.test.{ts,tsx}', 'tests/**/*.test.{ts,tsx}']
    - test.coverage.reporter: ['text', 'lcov']

    Step 3 — Create src/test-setup.ts:
    Import '@testing-library/jest-dom'. This file runs before each test and adds jest-dom matchers (toBeInTheDocument, etc.).

    Step 4 — Add test script to package.json scripts:
    "test": "vitest run"
    "test:watch": "vitest"
    "test:coverage": "vitest run --coverage"

    CRITICAL: vitest.config.ts is separate from vite.config.ts — Vitest in project mode uses vitest.config.ts if present, otherwise falls back to vite.config.ts. Having a separate vitest.config.ts avoids importing @tailwindcss/vite in the test environment (which can cause issues in jsdom).
    CRITICAL: test.globals: true is needed for describe/it/expect without explicit imports in test files.
  </action>

  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && pnpm vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - vitest.config.ts exists with test.environment: 'jsdom' and test.globals: true
    - src/test-setup.ts exists and imports '@testing-library/jest-dom'
    - package.json scripts contains "test": "vitest run"
    - pnpm vitest run exits without configuration errors (test count may be 0 if test files don't exist yet — that is acceptable here)
    - vitest, @testing-library/react, @testing-library/jest-dom, jsdom are in package.json devDependencies
  </acceptance_criteria>

  <done>Vitest installed, vitest.config.ts with jsdom environment, src/test-setup.ts with jest-dom matchers, package.json test scripts added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write unit tests for authStore, env, ProtectedRoute, Axios client, and app smoke test</name>

  <read_first>
    C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-RESEARCH.md — Validation Architecture section (full test map with file paths and expected behaviors)
    C:/Users/shift/Desktop/Frontend/src/stores/authStore.ts — authStore implementation
    C:/Users/shift/Desktop/Frontend/src/env.ts — env validation implementation
    C:/Users/shift/Desktop/Frontend/src/components/ui/ProtectedRoute.tsx — ProtectedRoute implementation
    C:/Users/shift/Desktop/Frontend/src/api/client.ts — Axios client implementation
    C:/Users/shift/Desktop/Frontend/src/main.tsx — App entry (for smoke test)
    C:/Users/shift/Desktop/Frontend/src/router.tsx — router
  </read_first>

  <behavior>
    Test: authStore initial state
    - expect(useAuthStore.getState().status).toBe('loading')
    - expect(useAuthStore.getState().user).toBeNull()
    - expect(useAuthStore.getState().accessToken).toBeNull()

    Test: authStore setAuth transitions to 'authenticated'
    - after setAuth({ id: '1', email: 'a@b.com', ... }, 'token123')
    - expect(useAuthStore.getState().status).toBe('authenticated')
    - expect(useAuthStore.getState().accessToken).toBe('token123')

    Test: authStore clearAuth transitions to 'unauthenticated'
    - after clearAuth()
    - expect(useAuthStore.getState().status).toBe('unauthenticated')
    - expect(useAuthStore.getState().accessToken).toBeNull()

    Test: authStore has no persist middleware
    - expect(useAuthStore.persist).toBeUndefined()

    Test: env validation throws on missing VITE_API_BASE_URL
    - mock import.meta.env with no VITE_API_BASE_URL
    - expect(() => import('../env')).rejects or throws with "VITE_API_BASE_URL"

    Test: ProtectedRoute shows Loader2 when status='loading'
    - mock useAuthStore to return { status: 'loading' }
    - render(<MemoryRouter><ProtectedRoute /></MemoryRouter>)
    - expect screen.getByTestId or role-based check for spinner to be in document

    Test: ProtectedRoute redirects to /login when status='unauthenticated'
    - mock useAuthStore to return { status: 'unauthenticated' }
    - render in MemoryRouter, check location changed to /login

    Test: Axios client attaches Authorization header when token present
    - mock useAuthStore.getState() to return accessToken: 'test-token'
    - mock axios interceptor
    - expect Authorization header = 'Bearer test-token' on outgoing request

    Test: Smoke — app renders without crashing
    - render app root with mocked i18n and mocked rawClient.post
    - expect no thrown errors

    Test: i18n initializes and resolves appName
    - configure i18next with initReactI18next in test mode using inline resources: { en: { common: { appName: 'Staccato' } } }
    - call i18next.init({ ... }) and await resolution
    - expect i18next.t('common.appName') === 'Staccato'
  </behavior>

  <action>
    Create the following test files:

    FILE: src/stores/__tests__/authStore.test.ts
    Import { useAuthStore } from '@/stores/authStore'. Before each test, call useAuthStore.setState({ status: 'loading', user: null, accessToken: null }) to reset state. Write describe block 'authStore' with tests for: initial state (status 'loading', user null, accessToken null), setAuth (status 'authenticated', accessToken set), clearAuth (status 'unauthenticated', user/token null), no persist middleware (useAuthStore.persist is undefined).

    FILE: src/__tests__/env.test.ts
    Test env validation. Import { z } from 'zod'. Since env.ts reads import.meta.env at module load time, test the Zod schema logic in isolation. Import the envSchema or recreate it in the test, then test: valid input passes, missing VITE_API_BASE_URL causes safeParse to fail with a fieldErrors entry for VITE_API_BASE_URL, invalid URL format causes failure.

    FILE: src/components/ui/__tests__/ProtectedRoute.test.tsx
    Import { render, screen } from '@testing-library/react'. Import { MemoryRouter, Route, Routes } from 'react-router'. Import { ProtectedRoute } from '@/components/ui/ProtectedRoute'. Import { useAuthStore } from '@/stores/authStore'. Use vi.mock('@/stores/authStore', ...) to control status.

    Tests:
    - 'renders spinner when status is loading': mock returns 'loading', render ProtectedRoute in MemoryRouter, expect Lucide Loader2 (look for animate-spin class or svg element)
    - 'redirects to /login when unauthenticated': mock returns 'unauthenticated', render inside MemoryRouter starting at /app/notebooks, check that rendered output navigates (use location tracking in MemoryRouter)
    - 'renders outlet when authenticated': mock returns 'authenticated', render with a child route, expect child content visible

    FILE: src/pages/__tests__/loginRegisterRedirect.test.tsx (per CHK-01 / ROADMAP SC2)
    Import { render } from '@testing-library/react'. Import { MemoryRouter } from 'react-router'. Import LoginPage from '@/pages/LoginPage'. Import RegisterPage from '@/pages/RegisterPage'. Use vi.mock('@/stores/authStore') to control status.

    Tests:
    - 'LoginPage redirects to /app/notebooks when authenticated': mock useAuthStore returns status 'authenticated', render <MemoryRouter initialEntries={['/login']}><LoginPage /></MemoryRouter>, expect a <Navigate> to '/app/notebooks' (check that the page does not render its stub content — use a spy on location changes or check that the stub text is not in the document)
    - 'RegisterPage redirects to /app/notebooks when authenticated': same pattern for RegisterPage
    - 'LoginPage renders normally when unauthenticated': mock status 'unauthenticated', render LoginPage, expect stub content visible (not redirected)

    FILE: src/api/__tests__/client.test.ts
    Import { client } from '@/api/client'. Use vi.mock('@/stores/authStore') to control accessToken. Use vi.mock('i18next', ...) to stub language as 'en'. Use axios-mock-adapter or vi.fn() on the interceptor config. Test: when accessToken is 'test-token', a request has Authorization: 'Bearer test-token'. When accessToken is null, no Authorization header is attached.

    FILE: tests/smoke/app-boots.test.tsx
    Import { render } from '@testing-library/react'. Use the following mock specifiers (module aliases, not CWD-relative paths):
    - vi.mock('@/api/rawClient', () => ({ rawClient: { post: vi.fn().mockRejectedValue(new Error('no cookie')) } }))
    - vi.mock('@/stores/authStore', () => ({ useAuthStore: vi.fn(() => ({ status: 'unauthenticated', user: null, accessToken: null })) }))
    - vi.mock('@/i18n', () => ({}))
    Render <RouterProvider router={router} /> inside a QueryClientProvider with a test QueryClient. Expect no thrown errors and document.body is not empty.

    FILE: src/__tests__/i18n.test.ts
    Import i18next from 'i18next'. Import initReactI18next from 'react-i18next'. Do NOT import from the app's src/i18n.ts module — configure i18next inline in the test to avoid side effects from the HTTP backend plugin. Initialize i18next with:
    - use(initReactI18next)
    - init({ lng: 'en', ns: ['common'], defaultNS: 'common', resources: { en: { common: { appName: 'Staccato' } } }, interpolation: { escapeValue: false } })
    Await the init call. Then assert: expect(i18next.t('common.appName')).toBe('Staccato').
    This test verifies that the i18next + react-i18next wiring resolves namespace keys correctly and serves as the automated check for ROADMAP SC5.

        NOTE: The smoke test may need to be simplified based on what can be mocked at the integration level. If a full render requires too much setup, a simpler version that just tests router configuration (checking route paths) is acceptable.

    IMPORTANT: Use vi.mock() not jest.mock(). Vitest uses vi global.
    IMPORTANT: Reset authStore state in beforeEach() to avoid test bleeding.
    IMPORTANT: For ProtectedRoute tests, mock @/stores/authStore at the module level using vi.mock.
  </action>

  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && pnpm vitest run --reporter=verbose 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - pnpm vitest run exits 0 (all tests pass)
    - src/stores/__tests__/authStore.test.ts exists and contains at least 4 tests
    - src/__tests__/env.test.ts exists and contains at least 2 tests (valid + invalid input)
    - src/components/ui/__tests__/ProtectedRoute.test.tsx exists and tests all 3 status branches
    - src/pages/__tests__/loginRegisterRedirect.test.tsx exists and verifies LoginPage and RegisterPage redirect authenticated users to /app/notebooks (ROADMAP SC2)
    - src/api/__tests__/client.test.ts exists and tests Authorization header attachment
    - tests/smoke/app-boots.test.tsx exists and passes without thrown errors
    - src/__tests__/i18n.test.ts exists and asserts i18next.t('common.appName') === 'Staccato' using inline resources
    - No test uses 'loading' as the initial status expectation incorrectly — authStore truly starts with 'loading' per D-04
    - No test file uses jest.mock() (must use vi.mock() — Vitest global)
  </acceptance_criteria>

  <done>All 8 test files created and passing: authStore (4 tests), env validation (2+ tests), i18n (1 test), ProtectedRoute (3 tests), login/register redirect (3 tests), Axios client (2 tests), smoke (1 test). pnpm vitest run exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → production code | Tests mock external dependencies; mocks must not accidentally test themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | Test mocks masking real behavior | mitigate | Tests for authStore use the real store (no mock); only external dependencies (rawClient, i18next) are mocked in smoke tests; interceptor tests use minimal mocking of store state only |
| T-01-18 | Information Disclosure | Test output in CI logs | accept | Test files contain no real secrets; env tests use placeholder strings; no credentials in test fixtures |
| T-01-SC | Tampering | npm/pnpm installs | mitigate | vitest, @testing-library/react, jsdom verified as Approved ecosystem packages; jsdom from jsdom/jsdom (2013, established) |
</threat_model>

<verification>
Final Phase 1 verification — all 5 success criteria from ROADMAP.md:

1. pnpm vitest run exits 0 — all tests pass
2. pnpm build exits 0 — zero TypeScript errors and zero lint errors
3. pnpm dev starts — navigating to /login shows LoginPage shell, /register shows RegisterPage shell, /app/notebooks shows spinner then /login redirect (no cookie)
4. Verify authStore has no persist: grep -c "persist" src/stores/authStore.ts returns 0
5. Verify i18n: pnpm vitest run src/__tests__/i18n.test.ts (if created) or manually: after pnpm dev, browser console shows no i18next errors; Accept-Language header is present on network requests

Phase 1 Success Criteria Check:
- [SC1] pnpm dev starts without errors; pnpm build completes with zero TypeScript errors: verify pnpm build 2>&1 | grep -c "error" returns 0
- [SC2] / redirects unauthenticated → /login; authenticated → /app/notebooks: ProtectedRoute test covers this
- [SC3] All routes render shells without 404s: smoke test covers this
- [SC4] useAuthStore has no persist middleware; store resets to 'loading' on reload: authStore test covers no persist; manual verification: page reload → status briefly 'loading' → clearAuth()
- [SC5] t('common.appName') resolves to 'Staccato'; Accept-Language header present: public/locales/en/common.json has appName; client.ts interceptor sets Accept-Language
</verification>

<success_criteria>
- pnpm vitest run exits 0 (all tests pass)
- pnpm build exits 0 (zero TypeScript errors)
- 8 test files exist and pass
- authStore test confirms no persist middleware
- ProtectedRoute test confirms all 3 status branches
- Phase 1 is complete — ready for /gsd:verify-phase
</success_criteria>

<output>
Create C:/Users/shift/Desktop/Frontend/.planning/phases/01-foundation/01-05-SUMMARY.md when done.
</output>
