---
phase: 05-lessons-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/features/lessons/api/lessonsApi.ts
  - src/features/lessons/api/lessonPagesApi.ts
  - public/locales/en/lessons.json
  - public/locales/hu/lessons.json
autonomous: true
requirements:
  - LES-01
  - LES-02
  - LES-03
  - LES-04
  - PAGE-01
  - PAGE-02

must_haves:
  truths:
    - "Lesson, LessonPage, CreateLessonPayload, UpdateLessonPayload interfaces are exported from src/types/index.ts"
    - "getLessons, getLesson, createLesson, updateLesson, deleteLesson are exported from lessonsApi.ts"
    - "getLessonPages, addPage, deletePage are exported from lessonPagesApi.ts"
    - "All API functions use the shared client from @/api/client — no ad-hoc axios.create"
    - "All type-only imports use 'import type' syntax (verbatimModuleSyntax)"
    - "en/lessons.json contains all required namespace keys for Plans 02–04"
    - "hu/lessons.json mirrors en structure with all string values set to __HU_TODO__"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Lesson domain types"
      exports: ["Lesson", "LessonPage", "CreateLessonPayload", "UpdateLessonPayload"]
    - path: "src/features/lessons/api/lessonsApi.ts"
      provides: "Lesson CRUD API functions"
      exports: ["getLessons", "getLesson", "createLesson", "updateLesson", "deleteLesson"]
    - path: "src/features/lessons/api/lessonPagesApi.ts"
      provides: "LessonPage add/delete API functions"
      exports: ["getLessonPages", "addPage", "deletePage"]
    - path: "public/locales/en/lessons.json"
      provides: "Complete English translations for the lessons feature"
    - path: "public/locales/hu/lessons.json"
      provides: "Hungarian translation stubs (all __HU_TODO__)"
  key_links:
    - from: "src/features/lessons/api/lessonsApi.ts"
      to: "src/api/client.ts"
      via: "named import"
      pattern: "import.*client.*from '@/api/client'"
    - from: "src/features/lessons/api/lessonsApi.ts"
      to: "src/types/index.ts"
      via: "type-only import"
      pattern: "import type.*Lesson.*from '@/types'"
    - from: "src/features/lessons/api/lessonPagesApi.ts"
      to: "src/api/client.ts"
      via: "named import"
      pattern: "import.*client.*from '@/api/client'"
    - from: "src/features/lessons/api/lessonPagesApi.ts"
      to: "src/types/index.ts"
      via: "type-only import"
      pattern: "import type.*LessonPage.*from '@/types'"
---

<objective>
Establish all shared infrastructure for Phase 5 — Lessons & Pages. This plan is purely additive: no user-visible changes ship. All downstream plans (02, 03, 04) depend on the types, API modules, and locale files created here.

Output: Five files created or updated — types, two API modules, two locale files.
</objective>

<execution_context>
@C:/Users/shift/Desktop/Frontend/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shift/Desktop/Frontend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/shift/Desktop/Frontend/.planning/PROJECT.md
@C:/Users/shift/Desktop/Frontend/.planning/ROADMAP.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/05-lessons-pages/05-CONTEXT.md
@C:/Users/shift/Desktop/Frontend/.planning/REQUIREMENTS.md
@C:/Users/shift/Desktop/Frontend/.planning/phases/05-lessons-pages/05-RESEARCH.md
</context>

<interfaces>
<!-- Current state of files this plan modifies. -->

From src/types/index.ts (current — append after the existing content):
```typescript
export interface UserProfile { ... }
export interface Notebook { ... }
export interface CreateNotebookPayload { ... }
export interface UpdateNotebookPayload { ... }
export const COVER_COLORS = [...] as const
export type CoverColor = (typeof COVER_COLORS)[number]
export const NOTEBOOK_STYLE_PRESETS = [...] as const
export type NotebookStylePreset = (typeof NOTEBOOK_STYLE_PRESETS)[number]
export const NOTEBOOK_PAGE_SIZES = [...] as const
export type NotebookPageSize = (typeof NOTEBOOK_PAGE_SIZES)[number]
```

From src/features/notebooks/api/notebooksApi.ts (template pattern to follow):
```typescript
import { client } from '@/api/client'
import type { Notebook, CreateNotebookPayload, UpdateNotebookPayload } from '@/types'

export async function getNotebook(id: string): Promise<Notebook> {
  const { data } = await client.get<Notebook>(`/notebooks/${id}`)
  return data
}
// ... etc
```

From public/locales/en/lessons.json (current — incomplete, replace entirely):
```json
{
  "create": { "title": "New Lesson", "titleLabel": "Lesson title", "submitButton": "Create lesson" },
  "actions": { "rename": "Rename lesson", "delete": "Delete lesson", "addPage": "Add page" },
  "pageCount": "{{count}} page",
  "pageCount_plural": "{{count}} pages"
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Lesson types to src/types/index.ts</name>
  <read_first>src/types/index.ts</read_first>
  <action>
Read src/types/index.ts to confirm its current content. Append the following four interfaces AFTER all existing content — do NOT remove or alter any existing definitions.

Add in order:

1. `Lesson` interface:
```typescript
export interface Lesson {
  id: string
  notebookId: string
  title: string
  pageCount: number
  globalPageStart: number
  createdAt: string
  updatedAt: string
}
```

2. `LessonPage` interface:
```typescript
export interface LessonPage {
  id: string
  lessonId: string
  pageNumber: number
  globalPageNumber: number
  createdAt: string
}
```

3. `CreateLessonPayload` interface:
```typescript
export interface CreateLessonPayload {
  title: string
}
```

4. `UpdateLessonPayload` interface:
```typescript
export interface UpdateLessonPayload {
  title?: string
}
```

CRITICAL constraints:
- No `enum` — use `as const` unions if any union type is needed (none required here).
- All use `export interface`.
- `verbatimModuleSyntax` — no type re-export issues here (value declarations only).
- Preserve all existing exports untouched.
  </action>
  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- `grep "export interface Lesson " src/types/index.ts` returns a match (with trailing space to avoid LessonPage)
- `grep "export interface LessonPage " src/types/index.ts` returns a match
- `grep "export interface CreateLessonPayload" src/types/index.ts` returns a match
- `grep "export interface UpdateLessonPayload" src/types/index.ts` returns a match
- `grep "export interface Notebook " src/types/index.ts` still returns a match (unchanged)
- `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Lesson, LessonPage, CreateLessonPayload, UpdateLessonPayload interfaces exported from src/types/index.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/features/lessons/api/lessonsApi.ts</name>
  <read_first>src/api/client.ts, src/features/notebooks/api/notebooksApi.ts</read_first>
  <action>
Create the directory path implicitly by writing the file at src/features/lessons/api/lessonsApi.ts.

Write this exact content:

```typescript
import { client } from '@/api/client'
import type { Lesson, CreateLessonPayload, UpdateLessonPayload } from '@/types'

export async function getLessons(notebookId: string): Promise<Lesson[]> {
  const { data } = await client.get<Lesson[]>(`/notebooks/${notebookId}/lessons`)
  return data
}

export async function getLesson(id: string): Promise<Lesson> {
  const { data } = await client.get<Lesson>(`/lessons/${id}`)
  return data
}

export async function createLesson(notebookId: string, payload: CreateLessonPayload): Promise<Lesson> {
  const { data } = await client.post<Lesson>(`/notebooks/${notebookId}/lessons`, payload)
  return data
}

export async function updateLesson(id: string, payload: UpdateLessonPayload): Promise<Lesson> {
  const { data } = await client.put<Lesson>(`/lessons/${id}`, payload)
  return data
}

export async function deleteLesson(id: string): Promise<void> {
  await client.delete(`/lessons/${id}`)
}
```

CRITICAL constraints:
- `import type { Lesson, CreateLessonPayload, UpdateLessonPayload }` — type-only import (verbatimModuleSyntax).
- Single `client` import from `@/api/client` — never axios directly or axios.create.
- No `enum`, no namespaces, no parameter properties.
  </action>
  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- File exists at src/features/lessons/api/lessonsApi.ts
- `grep "export async function deleteLesson" src/features/lessons/api/lessonsApi.ts` returns a match
- `grep "import type" src/features/lessons/api/lessonsApi.ts` returns a match
- `grep "axios" src/features/lessons/api/lessonsApi.ts` returns no match
- `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Five lesson API functions (get list, get one, create, update, delete) exported from lessonsApi.ts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create src/features/lessons/api/lessonPagesApi.ts</name>
  <read_first>src/features/lessons/api/lessonsApi.ts</read_first>
  <action>
Create src/features/lessons/api/lessonPagesApi.ts in the same directory as lessonsApi.ts.

Write this exact content:

```typescript
import { client } from '@/api/client'
import type { LessonPage } from '@/types'

export async function getLessonPages(lessonId: string): Promise<LessonPage[]> {
  const { data } = await client.get<LessonPage[]>(`/lessons/${lessonId}/pages`)
  return data
}

export async function addPage(lessonId: string): Promise<LessonPage> {
  const { data } = await client.post<LessonPage>(`/lessons/${lessonId}/pages`)
  return data
}

export async function deletePage(lessonId: string, pageId: string): Promise<void> {
  await client.delete(`/lessons/${lessonId}/pages/${pageId}`)
}
```

CRITICAL constraints:
- `import type { LessonPage }` — type-only import.
- Single `client` import from `@/api/client` only.
- `POST /lessons/{id}/pages` sends no request body (blank page — no second argument to client.post beyond the URL).
  </action>
  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
- File exists at src/features/lessons/api/lessonPagesApi.ts
- `grep "export async function addPage" src/features/lessons/api/lessonPagesApi.ts` returns a match
- `grep "export async function deletePage" src/features/lessons/api/lessonPagesApi.ts` returns a match
- `grep "import type" src/features/lessons/api/lessonPagesApi.ts` returns a match
- `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>getLessonPages, addPage, deletePage exported from lessonPagesApi.ts using the shared client.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Replace public/locales/en/lessons.json and public/locales/hu/lessons.json</name>
  <read_first>public/locales/en/lessons.json, public/locales/hu/lessons.json</read_first>
  <action>
Replace both locale files completely with expanded key sets.

For public/locales/en/lessons.json, write this exact JSON:
```json
{
  "list": {
    "title": "Lessons",
    "empty": "No lessons yet",
    "emptyHint": "Add your first lesson to start tracking your practice.",
    "newLessonButton": "New Lesson",
    "newSlotLabel": "+ New Lesson"
  },
  "pageCount": "{{count}} page",
  "pageCount_plural": "{{count}} pages",
  "create": {
    "title": "New Lesson",
    "titleLabel": "Lesson title",
    "titlePlaceholder": "e.g. Scales",
    "submitButton": "Create lesson"
  },
  "rename": {
    "title": "Rename Lesson",
    "titleLabel": "Lesson title",
    "submitButton": "Save"
  },
  "delete": {
    "title": "Delete \"{{name}}\"?",
    "description": "This can't be undone. All pages and content will be permanently removed.",
    "confirm": "Delete"
  },
  "deletePage": {
    "title": "Delete this page?",
    "description": "This can't be undone. All content on this page will be permanently removed.",
    "confirm": "Delete page"
  },
  "actions": {
    "open": "Open",
    "rename": "Rename",
    "delete": "Delete lesson",
    "addPage": "Add page",
    "deletePage": "Delete page",
    "backToLessons": "Lessons"
  },
  "page": {
    "pageOf": "Page {{current}} of {{total}}",
    "globalPage": "Notebook p. {{number}}"
  },
  "warnings": {
    "tenPages": "This lesson has 10 pages — consider splitting it into multiple lessons."
  },
  "errors": {
    "createFailed": "Failed to create lesson. Please try again.",
    "updateFailed": "Failed to update lesson. Please try again.",
    "deleteFailed": "Failed to delete lesson. Please try again.",
    "addPageFailed": "Failed to add page. Please try again.",
    "deletePageFailed": "Failed to delete page. Please try again.",
    "loadFailed": "Failed to load lessons. Please try again."
  }
}
```

For public/locales/hu/lessons.json, write the exact same key structure with every string value replaced by "__HU_TODO__". The nested structure must be identical (same keys, same depth).
  </action>
  <verify>
    <automated>cd "C:/Users/shift/Desktop/Frontend" && node -e "const en = require('./public/locales/en/lessons.json'); const hu = require('./public/locales/hu/lessons.json'); console.log('en top-level keys:', Object.keys(en).join(', ')); console.log('match:', JSON.stringify(Object.keys(en).sort()) === JSON.stringify(Object.keys(hu).sort()))"</automated>
  </verify>
  <acceptance_criteria>
- Both files parse as valid JSON
- EN file contains: list, pageCount, pageCount_plural, create, rename, delete, deletePage, actions, page, warnings, errors top-level keys
- HU file has identical top-level keys with all string values "__HU_TODO__"
- EN "delete.title" contains the "{{name}}" interpolation placeholder
- EN "page.pageOf" contains "{{current}}" and "{{total}}" placeholders
  </acceptance_criteria>
  <done>English lessons.json fully populated with all Phase 5 translation keys; Hungarian file mirrors structure with __HU_TODO__ stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | All lesson/page API calls carry the JWT access token via the shared Axios interceptor (set up in Phase 2); no additional trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | lessonsApi.ts payloads | accept | Backend validates notebook ownership and auth; frontend sends JWT via Axios interceptor |
| T-05-02 | XSS | locale JSON string interpolation | mitigated | i18next t() escapes interpolated values; no raw HTML in locale strings |
| T-05-SC | Tampering | pnpm installs | accept | No new packages added in this plan |
</threat_model>

<verification>
After all 4 tasks complete:

1. `pnpm tsc --noEmit` — exits 0, zero type errors
2. `grep "export interface Lesson " src/types/index.ts` — returns match
3. `grep "export interface LessonPage " src/types/index.ts` — returns match
4. `grep "export async function getLessons" src/features/lessons/api/lessonsApi.ts` — returns match
5. `grep "export async function addPage" src/features/lessons/api/lessonPagesApi.ts` — returns match
6. `node -e "require('./public/locales/en/lessons.json')"` — exits 0
7. `node -e "require('./public/locales/hu/lessons.json')"` — exits 0
</verification>

<success_criteria>
1. All five files exist and TypeScript compiles without errors.
2. The five lesson API functions mirror the notebooksApi.ts pattern (shared client, type-only imports).
3. The three page API functions mirror the same pattern.
4. EN locale file contains all keys needed by Plans 02, 03, and 04 (list, create, rename, delete, deletePage, actions, page, warnings, errors).
5. HU locale file mirrors EN structure exactly, all values "__HU_TODO__".
</success_criteria>

<output>
Create C:/Users/shift/Desktop/Frontend/.planning/phases/05-lessons-pages/05-01-SUMMARY.md when done.
</output>
